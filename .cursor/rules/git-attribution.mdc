---
description: Git commits must attribute only the student author; no Cursor or AI co-author lines.
alwaysApply: true
---

# Git attribution (ESA)

- Use **only** this identity for every commit in this repository:
  - **Name:** `sadek17481748`
  - **Email:** `sadek_j8@outlook.com`
- Before committing, ensure local config matches (or pass `-c user.name` / `-c user.email`):
  - `git config user.name` → `sadek17481748`
  - `git config user.email` → `sadek_j8@outlook.com`
- **Do not** add `Co-authored-by:`, `Signed-off-by:`, or any trailer that names Cursor, bots, or IDE vendors.
- **Do not** amend commit messages to include automated tooling as authors.

The coursework record must show the learner as the sole commit author.
